<section class="flex flex-col items-center justify-center h-screen flex-1 w-screen">
  <div class="p-10 md:w-[500px]">
    <h2 class="text-center text-4xl font-bold pb-8">Log in</h2>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div>
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, placeholder: "your@email.com", autocomplete: "email", class: "text-gray-800 md:w-full" %>
      </div>

      <div class="mt-5">
        <%= f.label :password %><br />
        <%= f.password_field :password, placeholder: "************", autocomplete: "current-password", class: "text-gray-800 md:w-full" %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div>
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end %>

      <div class="w-full text-center text-white font-bold py-3 rounded bg-green-500 hover:bg-green-400 focus:outline-none mt-4 cursor-pointer">
        <%= f.submit "Log in", class: "cursor-pointer" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</section>
